REGLAS GENERALES
- All content you generate for rows 5+ must be in ENGLISH.
- Do NOT use the character ';'. If the input contains ';', replace it with ', '.
- You will receive: the recipe URL, the recipe title, the full ingredient list and the step-by-step instructions.
- You must output ONE ROW PER STEP of the recipe.
- Each row has 14 columns (A�N), separated by |||.
- Work in the sheet named exactly 'Algorithm Output' and it must be the first tab. If not, stop and notify in chat.

DEFINICI�N DE COLUMNAS (A�N)

A: Recipe ID.
- Integer: 1, 2, 3, ...
- Same ID for all steps of the same recipe.

B: Recipe URL.
- The same URL in all rows of the same recipe.

C: Recipe title.
- In UPPERCASE.
- Same title in all rows of the same recipe.

D: Full ingredient list.
- All ingredients of the recipe.
- One ingredient per line (use line breaks).
- Repeat the full list in all rows of the same recipe.

E: Short description of the step.
- A brief description of what happens in that step.
- Plain text (italics will be applied later in Excel, you just output text).

F: Hob container number.
- Number the containers used on the hob (1, 2, 3, ...), restarting at 1 for each recipe.
- If the same pan/pot is reused, repeat its number.
- If the utensil does NOT use the hob (oven, microwave, airfryer, bowl, etc.), write N/A.

G: Step number.
- 1, 2, 3, ... for each step of the recipe.
- Restart at 1 for each recipe.

H: Ingredients used in that step.
- Only the ingredients actually used in that step.
- One ingredient per line (use line breaks).
- Do NOT use ';'.

I: New ingredient added in that step.
- If a new ingredient appears in this step for the first time, write that ingredient.
- If no new ingredient is added, repeat the last new ingredient used in previous steps of the same recipe.
- If it is the first step and there is no clear new ingredient, write N/A.

J: Main ingredient of the step.
- Exactly ONE ingredient.
- It must appear in column H.
- If there is a main protein, choose that. Otherwise choose the most relevant ingredient for that step.

K: Cooking process.
- Choose ONE from this normalized list: Fry, Shallow fry, Deep fry, Simmering, Mix Boil, Steaming, Reducing, Bain Marie, Pressure cooker, Sous vide, Grill, Pan Fry, Melting, Cut, Peel, Wash, Sliced.
- If none fits, write: NEW: <suggested process>.

L: Frying texture.
- ONLY if column K is exactly 'Fry':
  - Write 'crispy' or 'soft' depending on the final texture.
- In all other cases, leave the cell EMPTY (just nothing).

M: Step duration in minutes.
- If the duration appears explicitly in the text, convert it to minutes and round to the nearest integer.
- If the duration is NOT explicit, write N/A (in uppercase).

N: Lid usage.
- 'yes' if the step uses a lid placed on the container.
- 'no' if the step does not use a lid.
- 'N/A' if the container is a type without lid (e.g. bowl, tray, etc.).

FORMATO DE SALIDA

- You MUST output ONE row per recipe step.
- Each row MUST have EXACTLY 14 columns (A�N) separated by ' ||| '.
- Do NOT add headers, explanations, comments, or extra text.
- Do NOT mention Excel, files, colors, borders, fonts or links.
- Use ONLY the information from the recipe text. If something is not explicit but can be reasonably inferred, infer it. If it cannot be inferred, use N/A.